data/raw/
data/scored/
# keep the empty data dirs in the tree so the scaffold survives a clone
!data/raw/.gitkeep
!data/scored/.gitkeep
__pycache__/
*.pyc
.venv/
venv/

# local Claude Code harness settings (not study work)
.claude/settings.local.json

# secrets / local endpoints (never commit; .env.example is the tracked template)
.env

# phase2 operational logs (progress spam, not study artifacts)
phase2/*.log

# OS / tooling cruft
.DS_Store
.Rhistory
